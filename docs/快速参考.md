# 🚀 Skill Hub 快速参考卡

## ⚡ 一键启动

```cmd
cd E:\projects\AIs\skill-creator
.venv\Scripts\python.exe -m skill_hub.cli
```

访问: http://localhost:8000/docs

---

## 📋 常用命令

```cmd
# 切换到项目根目录
cd E:\projects\AIs\skill-creator

# 启动开发服务器
.venv\Scripts\python.exe -m skill_hub.cli

# 初始化数据库
mysql -uroot -p skill_hub < create_tables.sql

# 启动 Docker 服务
docker-compose -f docker-compose.simple.yml up -d

# 停止 Docker 服务
docker-compose -f docker-compose.simple.yml down

# 查看 Docker 状态
docker-compose -f docker-compose.simple.yml ps

# 查看 Docker 日志
docker-compose -f docker-compose.simple.yml logs -f
```

---

## 🌐 服务地址

| 服务 | 地址 | 凭据 |
|------|------|------|
| API 文档 | http://localhost:8000/docs | - |
| API 根路径 | http://localhost:8000 | - |
| MinIO 控制台 | http://localhost:9001 | minioadmin / minioadmin123 |
| MySQL | localhost:3306 | root / 你的密码 |
| Redis | localhost:6379 | - |

---

## 🔧 配置文件

| 文件 | 说明 |
|------|------|
| `.env` | 主配置文件（项目根目录） |
| `backend/.env` | 自动复制的配置文件 |
| `docker-compose.simple.yml` | Docker 服务配置 |
| `create_tables.sql` | 数据库表结构 |

---

## ❌ 快速故障排除

### 问题 1: 找不到文件
```cmd
# 确保在项目根目录
cd E:\projects\AIs\skill-creator
```

### 问题 2: 端口被占用
```cmd
# 停止 Docker
docker-compose -f docker-compose.simple.yml down

# 查看端口占用
netstat -ano | findstr :8000
```

### 问题 3: 数据库连接失败
```cmd
# 检查 MySQL 是否运行
mysql -uroot -p -e "SELECT 1"

# 检查数据库是否存在
mysql -uroot -p -e "SHOW DATABASES LIKE 'skill_hub'"
```

### 问题 4: dev.exe 被锁定
```cmd
# 使用 Python 直接运行
.venv\Scripts\python.exe -m skill_hub.cli
```

---

## 📁 项目结构

```
E:\projects\AIs\skill-creator\
├── .env                    ← 主配置
├── backend/
│   ├── .env               ← 自动复制
│   └── app/
│       ├── main.py        ← FastAPI 入口
│       ├── api/           ← API 端点
│       ├── models/        ← 数据模型
│       ├── schemas/       ← Pydantic 模式
│       └── services/      ← 业务逻辑
├── skill_hub/
│   └── cli.py             ← CLI 命令
└── create_tables.sql      ← 数据库表
```

---

## 🎯 开发流程

```cmd
# 1. 启动 Docker
docker-compose -f docker-compose.simple.yml up -d

# 2. 启动开发服务器
.venv\Scripts\python.exe -m skill_hub.cli

# 3. 访问 API 文档
# http://localhost:8000/docs

# 4. 开发...

# 5. 停止服务（Ctrl+C）
```

---

## 📚 详细文档

- [修复完成.md](修复完成.md) - 最新修复说明
- [现在就开始.md](现在就开始.md) - 详细启动指南
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - 故障排除
- [QUICK_START_UV.md](QUICK_START_UV.md) - UV 命令指南

---

## 💡 记住这些

1. **永远在项目根目录运行命令**
2. **使用 `.venv\Scripts\python.exe -m skill_hub.cli` 启动**
3. **首次使用需要初始化数据库**
4. **metadata 字段已重命名为 skill_metadata**

---

## 🆘 需要帮助？

1. 查看 [修复完成.md](修复完成.md)
2. 查看 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
3. 检查服务状态: `docker-compose -f docker-compose.simple.yml ps`
4. 查看日志: `docker-compose -f docker-compose.simple.yml logs -f`

---

**祝开发愉快！** 🎉
